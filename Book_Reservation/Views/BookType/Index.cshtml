@model IEnumerable<Book_Reservation.Models.BookType>

@{
    ViewData["Title"] = "Index";
}

<div class="container-fluid">
    <div class="pricing-header p-2 pb-md-2 mx-auto text-center">
        <h1 class="display-6 text-center mb-4">Booktype Index</h1>
        <p class="fs-5 text-body-secondary">
            <a class="btn btn-primary" asp-action="Create">
                <i class="fa-sharp fa-light fa-plus fa-2xl" style="color: #ffffff;"></i> Create New
            </a>
        </p>
    </div>

    <div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.BooktypeId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BooktypeName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BooktypeStatus)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.BooktypeId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BooktypeName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BooktypeStatus)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.BooktypeId" class="btn btn-sm btn-primary">
                                <i class="fa fa-sharp fa-light fa-pen"></i> Edit
                            </a>
                            <a asp-action="Details" asp-route-id="@item.BooktypeId" class="btn btn-sm btn-info">Details</a>
                            <a asp-action="Delete" asp-route-id="@item.BooktypeId" class="btn btn-sm btn-danger">
                                <i class="fa fa-sharp fa-light fa-trash-can"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>